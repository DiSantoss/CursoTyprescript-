console.log('Testando 1, 2, 3, 4')

// no arquivo json, existem varios comandos comentados, comandos que nao estao funcionando, entao vamo aprender sobre esses caras.

//antes de começarmos a ver sobre esses itens, vamos ver uma opção fora do que ta aqui nesse json. mas mto importante para aprendermos.

// é o exclude e o include, vamos colocar eles fora do compilerOptions, fora do compiler mas dentro do objeto.